{
  "name": "coverage-test-reproducer",
  "version": "1.0.0",
  "type": "module",
  "script": {},
  "description": "Project to show c8 codecoverage bug",
  "main": "main.ts",
  "scripts": {
    "test": "vitest run -r src --coverage",
    "build": "tsc --build && node dist/main.js"
  },
  "author": "Ondrej Dockal",
  "license": "apache-2.0",
  "devDependencies": {
    "@vitest/coverage-c8": "^0.29.5",
    "vitest": "^0.29.5"
  }
}
